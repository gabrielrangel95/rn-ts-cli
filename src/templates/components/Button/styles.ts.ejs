import styled from 'styled-components/native';
import { colors, metrics } from '@styles';

interface IButtonProps {
  secondary?: boolean;
}

export const ButtonContainer = styled.TouchableOpacity`
  width: ${metrics.BASE_WIDTH}px;
  height: ${metrics.BASE_HEIGHT}px;
  background-color: ${(props: IButtonProps) => props.secondary ? colors.primary : colors.black};
  align-items: center;
  justify-content: center;
`;

export const ButtonText = styled.Text`
  color: ${(props: IButtonProps) => props.secondary ? colors.black : colors.white};
  font-size: ${metrics.FONT_SMALL};
  font-weight: bold;
`;
