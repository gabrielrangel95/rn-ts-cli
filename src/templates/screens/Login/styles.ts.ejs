import styled from 'styled-components/native';
import { metrics, colors } from '@styles';

export const RootContainer = styled.View`
  display: flex;
  height: ${metrics.SCREEN_HEIGHT};
  width: ${metrics.SCREEN_WIDTH};
  background-color: ${colors.white};
  align-items: center;
  justify-content: center;
`;

export const FormContainer = styled.View`
  display: flex;
  align-items: center;
  justify-content: center;
  padding-top: ${2 * metrics.BASE_PADDING}px;
  padding-bottom: ${2 * metrics.BASE_PADDING}px;
`;


export const NoAccountTextContainer = styled.TouchableOpacity`
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: ${2 * metrics.BASE_PADDING}px;
  padding: 12px;
`;

export const NoAccountText = styled.Text`
  font-size: ${metrics.FONT_SMALL};
  color: ${colors.black};
  border-bottom-width: 1px;
  border-color: ${colors.black};
  border-style: solid;
`;
